---
title: Codex guidebook 12｜调用链索引
date: 2026-04-12
status: draft
purpose: appendix
---

# 12｜调用链索引

这份附录不试图覆盖全仓所有调用链，
只收录当前最值得反复回看的 **主链与专题链**。

阅读方法：
- 如果你已经知道想看哪条主线，从这里直接跳
- 如果你只想知道“这个系统最该顺着哪几个函数读下去”，从这里选一条

---

## 1. 系统总图主链

目标：先建立“壳层 / 入口层 / frontend / control plane / runtime owner”这张总图。

建议顺序：
1. `cli/src/main.rs`
2. `tui/src/lib.rs`
3. `tui/src/app_server_session.rs`
4. `app-server/src/message_processor.rs`
5. `core/src/lib.rs`
6. `core/src/thread_manager.rs`
7. `mcp-server/src/lib.rs`
8. `mcp-server/src/message_processor.rs`

对应正文：
- `01-系统总图与分层`

---

## 2. 状态持久化与恢复主链

目标：理解 rollout、SQLite、bridge、backfill、resume 到底怎么协作。

建议顺序：
1. `core/src/codex.rs`
2. `rollout/src/recorder.rs`
3. `rollout/src/state_db.rs`
4. `rollout/src/metadata.rs`
5. `state/src/lib.rs`
6. `state/src/runtime.rs`
7. `state/src/runtime/threads.rs`
8. `app-server/src/codex_message_processor.rs`

对应正文：
- `02-状态持久化与恢复`

---

## 3. app-server / listener / thread 主链

目标：理解 app-server 怎样把 core runtime 投影成 thread/turn 协议面。

建议顺序：
1. `app-server/src/codex_message_processor.rs`
2. `app-server/src/thread_state.rs`
3. `app-server/src/thread_status.rs`
4. `app-server/src/outgoing_message.rs`
5. `app-server/src/bespoke_event_handling.rs`
6. `app-server-protocol/src/protocol/thread_history.rs`

推荐的函数级顺序：
1. `ensure_listener_task_running_task(...)`
2. `apply_bespoke_event_handling(...)`
3. `handle_thread_listener_command(...)`
4. `handle_pending_thread_resume_request(...)`
5. `resolve_pending_server_request(...)`
6. `populate_thread_turns(...)`
7. `set_thread_status_and_interrupt_stale_turns(...)`
8. `resolve_thread_status(...)`

对应正文：
- `03-app-server与thread-turn主线`

---

## 4. turn-history 语义主链

目标：理解 event stream 如何变成 turn history，rollback / compaction / active turn overlay 如何成立。

建议顺序：
1. `app-server-protocol/src/protocol/thread_history.rs`
2. `core/src/thread_manager.rs`
3. `core/src/codex/rollout_reconstruction.rs`
4. `app-server/src/thread_state.rs`
5. `app-server/src/codex_message_processor.rs`

推荐的函数级顺序：
1. `build_turns_from_rollout_items(...)`
2. `ThreadHistoryBuilder::handle_event(...)`
3. `handle_user_message(...)`
4. `active_turn_snapshot(...)`
5. `Session::reconstruct_history_from_rollout(...)`

对应正文：
- `04-turn-history语义层`

---

## 5. unified-exec 主链

目标：理解执行请求如何进入 approval/policy/runtime，再变成 transcript / end event / process reconciliation 闭环。

建议顺序：
1. `core/src/tools/handlers/unified_exec.rs`
2. `core/src/tools/runtimes/unified_exec.rs`
3. `core/src/unified_exec/mod.rs`
4. `core/src/unified_exec/process_manager.rs`
5. `core/src/unified_exec/process.rs`
6. `core/src/unified_exec/async_watcher.rs`
7. `core/src/exec.rs`
8. `core/src/exec_env.rs`
9. `core/src/exec_policy.rs`

推荐的函数级顺序：
1. `UnifiedExecHandler::handle(...)`
2. `UnifiedExecRuntime::run(...)`
3. `open_session_with_exec_env(...)`
4. `UnifiedExecProcess::from_spawned(...)`
5. `store_process(...)`
6. `spawn_exit_watcher(...)`
7. `start_streaming_output(...)`
8. `process_chunk(...)`
9. `emit_exec_end_for_unified_exec(...)`
10. `emit_failed_exec_end_for_unified_exec(...)`
11. `resolve_aggregated_output(...)`
12. `refresh_process_state(...)`

对应正文：
- `05-unified-exec执行子系统`
- 参考草图：`02-call-chain-drafts/2026-04-11-codex主链草图-exec链.md`

---

## 6. capability system 主链

目标：理解 plugin / skills / MCP / apps / connectors 怎么组合成 capability substrate。

建议顺序：
1. `core/src/plugins/manager.rs`
2. `core/src/skills.rs`
3. `core/src/mcp.rs`
4. `core/src/mcp_skill_dependencies.rs`
5. `core/src/connectors.rs`
6. `app-server/src/codex_message_processor/plugin_app_helpers.rs`

对应正文：
- `06-capability与高级子系统`

---

## 7. model request 主链

目标：理解 `ModelClient` 怎么把 session/turn 语义装进 provider 请求。

建议顺序：
1. `core/src/client.rs`
2. `login/src/api_bridge.rs`
3. `model-provider-info/src/lib.rs`
4. `codex-api/src/common.rs`
5. `codex-api/src/provider.rs`
6. `codex-api/src/endpoint/responses.rs`
7. `codex-api/src/endpoint/responses_websocket.rs`

推荐的函数级顺序：
1. `ModelClient::new_session()`
2. `build_responses_request(...)`
3. `build_responses_options(...)`
4. `stream_responses_websocket(...)`
5. `stream_responses_http(...)`（或等价 HTTP path）
6. `current_client_setup()`

对应正文：
- `07-model-client与provider请求主链`

---

## 8. transport / backend boundary 主链

目标：分清 `codex-client`、`codex-api`、`backend-client` 三者关系。

建议顺序：
1. `codex-client/src/lib.rs`
2. `codex-client/src/request.rs`
3. `codex-client/src/transport.rs`
4. `codex-client/src/retry.rs`
5. `codex-api/src/lib.rs`
6. `codex-api/src/endpoint/session.rs`
7. `backend-client/src/client.rs`
8. `app-server/src/codex_message_processor.rs`
9. `cloud-requirements/src/lib.rs`

对应正文：
- `08-codex-client-codex-api与backend-client分层`

---

## 9. review / guardian 主链

目标：理解用户 review workflow 和 approval reviewer infrastructure 为什么是两套系统。

建议顺序：
1. `core/src/tasks/review.rs`
2. `core/src/guardian/review.rs`
3. `core/src/guardian/review_session.rs`
4. `core/src/codex_delegate.rs`
5. `core/src/tools/orchestrator.rs`
6. `core/src/tools/network_approval.rs`
7. `core/src/mcp_tool_call.rs`
8. `app-server/src/bespoke_event_handling.rs`
9. `analytics/src/events.rs`
10. `analytics/src/reducer.rs`

对应正文：
- `09-review工作流与guardian审查基础设施`

---

## 10. realtime / collab / memories / migration 主链

目标：理解 live control plane 与 startup adaptation layer。

### realtime / collab
建议顺序：
1. `core/src/realtime_conversation.rs`
2. `core/src/agent/control.rs`
3. `core/src/tools/handlers/multi_agents_v2/spawn.rs`
4. `.../message_tool.rs`
5. `.../wait.rs`
6. `.../close_agent.rs`
7. `app-server/src/bespoke_event_handling.rs`

### memories / migration
建议顺序：
1. `core/src/memories/mod.rs`
2. `core/src/memories/start.rs`
3. `core/src/memories/phase1.rs`
4. `core/src/memories/phase2.rs`
5. `state/src/runtime/memories.rs`
6. `core/src/external_agent_config.rs`
7. `app-server/src/external_agent_config_api.rs`

对应正文：
- `10-realtime-collab与memory迁移专题`

---

## 最后一句建议

如果你已经读完 01-10，下一步最划算的方式不是继续平扫仓库，而是：

- 先从这份调用链索引里挑一条
- 再回到 `01-source-notes/` 看对应函数级证据
- 只在正文还不够解释的时候，继续深挖具体函数
